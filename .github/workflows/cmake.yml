name: CMake

on: [push]

env:
  # Customize the CMake build type here (Release, Debug, RelWithDebInfo, etc.)
  BUILD_TYPE: Release

jobs:
  build:
    # The CMake configure and build commands are platform agnostic and should work equally
    # well on Windows or Mac.  You can convert this to a matrix build if you need
    # cross-platform coverage.
    # See: https://docs.github.com/en/actions/configuring-and-managing-workflows/configuring-a-workflow#configuring-a-build-matrix
    runs-on: ubuntu-latest

    steps:
    - name: Install Qt
      uses: jurplel/install-qt-action@v2
      with:
        version: 5.11.3

    # Build qmqtt
    - uses: actions/checkout@v2
      name: Checkout qmqtt
      with:
        path: qmqtt
        repository: ejvr/qmqtt

    - name: Create QMQTT Build Environment
      run: cmake -E make_directory ${{runner.workspace}}/qmqtt/build

    - name: Configure QMQTT CMake
      shell: bash
      working-directory: ${{runner.workspace}}/qmqtt/build
      run: cmake -DCMAKE_BUILD_TYPE=$BUILD_TYPE ..
 
    - name: Build & install QMQTT
      shell: bash
      working-directory: ${{runner.workspace}}/qmqtt/build
      run: cmake --build . --config $BUILD_TYPE --target install

    # Build qmqtt2qml
    - uses: actions/checkout@v2
      name: Checkout qmqtt2qml
      with:
        path: qmqqt2qml

    - name: Create Build Environment
      run: cmake -E make_directory ${{runner.workspace}}/qmqqt2qml/build

    - name: Configure QMQTT CMake
      shell: bash
      working-directory: ${{runner.workspace}}/qmqqt2qml/build
      run: cmake -DCMAKE_BUILD_TYPE=$BUILD_TYPE ..

    - name: Build
      shell: bash
      working-directory: ${{runner.workspace}}/qmqqt2qml/build
      run: cmake --build . --config $BUILD_TYPE
